package config

// config_reqpriority_integration.go wires request-priority middleware into the
// Config.Middlewares slice via the init() registered in reqpriority_config.go.
// No additional setup is required; the init() call in that file is sufficient.
